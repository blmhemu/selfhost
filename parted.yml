---

- name: Create partition for tranfer install
  hosts: nas0
  become: true
  tasks:
    - name: Create partition 1 on os_disk
      parted:
        device: /dev/disk/by-id/{{ os_disk }}
        number: 1
        state: present
        fs_type: ext4
        label: gpt